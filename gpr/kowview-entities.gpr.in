-- Build file for KOW_View - Web Framework for Ada Works.
--
-- author Eduardo Calderini Jr <ecalderini@ydeasolutions.com.br>
--
-- Repository information:
-- $Date: $
-- $Revision: $
-- $Author: $

with "kowconfig";
with "kowconfig-text";
with "kowent";
with "kowlib";
with "kowsec";
with "kowview";

with "aws";



project KOWView.Entities is

	version	:= $version;
	
	
	
	for Source_Dirs use ( $include_path ); 
	
	-- LIBRARY
	for Library_Dir use $lib_path;
	for Library_Name use "kowview-entities";
	for Library_Kind use KOWView'Library_Kind;
	for Library_Version use "libkowview-entities.so." & Version;
	for Externally_Built use "true";
	
	
	package Compiler is
		for Default_Switches ("ada") use ("-O2", "-gnat05");
	end Compiler;

	package Linker renames KOWLib.Linker;

end KOWView.Entities;

